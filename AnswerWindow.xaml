<Window x:Class="SnapMini.AnswerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SnapMini - Gemini Answer"
        Height="420" Width="480"
        Topmost="True"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Section 1: Input Text / OCR Text -->
        <TextBlock Grid.Row="0" Text="Captured Text:" FontWeight="Bold" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="QuestionBox" IsReadOnly="True" TextWrapping="Wrap"
                 MaxHeight="100" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12"/>

        <!-- Section 2: Gemini Answer -->
        <TextBlock Grid.Row="2" Text="Gemini Answer:" FontWeight="Bold" FontSize="13" Margin="0,0,0,4"/>
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="AnswerText" TextWrapping="Wrap" FontSize="14"/>
        </ScrollViewer>
    </Grid>
</Window>